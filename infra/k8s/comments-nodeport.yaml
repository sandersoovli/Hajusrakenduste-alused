apiVersion: v1
kind: Service
metadata:
  name: comments-node
spec:
  type: NodePort
  selector:
    app: comments
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30011
